version: '3.8'

services:
  db:
    image: mysql:8.0
    volumes:
      - db_data:/var/lib/mysql
      - ./sql/dev/products.sql:/docker-entrypoint-initdb.d/products.sql
  app:
    build: .
    # En dev no se monta el código local
